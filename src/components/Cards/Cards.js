// shortCut: 'rafc' + 'enter' => in order to write the code from App.js (columsn/ Cards /rows) directly in the Cards-folder/file

import React from "react";

const Cards = () => {
  return <div className="col-4">Cards</div>;
};

export default Cards;
